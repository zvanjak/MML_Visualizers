<Window x:Class="MML_ParametricCurve3D_Visualizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MML_ParametricCurve3D_Visualizer"
        xmlns:vizbase="clr-namespace:MML_VisualizersBase;assembly=MML_VisualizersBase"
        mc:Ignorable="d"
        Title="Parametric curve 3D visualizer" Height="800" Width="1200" 
        MouseLeftButtonDown="Window_MouseLeftButtonDown" MouseLeftButtonUp="Window_MouseLeftButtonUp" MouseRightButtonDown="Window_MouseRightButtonDown" MouseRightButtonUp="Window_MouseRightButtonUp" 
        MouseMove="Window_MouseMove" MouseWheel="Window_MouseWheel" KeyDown="Window_KeyDown">
  
  <Grid Background="White">
    <Grid.RowDefinitions>
      <RowDefinition Height="40"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="220" />
    </Grid.ColumnDefinitions>

    <TextBox x:Name="txtTitle" Grid.Column="0" TextWrapping="Wrap" Text="Title" VerticalAlignment="Center" Width="352" HorizontalAlignment="Center" FontSize="22" FontWeight="Bold" VerticalContentAlignment="Top" HorizontalContentAlignment="Center" Padding="0" BorderBrush="{x:Null}" IsReadOnly="True"/>

    <Viewport3D Name="myViewport3D"
                Grid.Row="1"
                Grid.Column="0"
                ClipToBounds="True"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch">
    </Viewport3D>
    
    <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,10,10,10">
      <GroupBox Header="Graph title" Margin="0,0,0,10">
        <TextBox x:Name="txtEditableTitle" TextWrapping="Wrap" AcceptsReturn="True" 
                 Height="50" Margin="5" VerticalScrollBarVisibility="Auto"
                 TextChanged="OnTitleTextChanged"/>
      </GroupBox>
      
      <GroupBox Header="Legend" Margin="0,0,0,10">
        <vizbase:LegendWidget x:Name="LegendWidgetControl" Margin="5"/>
      </GroupBox>
      
      <GroupBox Header="Animation" Margin="0,0,0,10">
        <StackPanel Margin="5">
          <Button x:Name="btnStartAnimation" Content="Start animation" HorizontalAlignment="Stretch" Margin="0,5" Click="btnStartAnimation_Click"/>
          <Grid Margin="0,5">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnPauseAnimation" Grid.Column="0" Content="Pause" Margin="0,0,2,0" Click="btnPauseAnimation_Click"/>
            <Button x:Name="btnResetAnimation" Grid.Column="1" Content="Reset" Margin="2,0,0,0" Click="btnResetAnimation_Click"/>
          </Grid>
          <Grid Margin="0,5">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <Label Content="Speed (pnts/sec):" Grid.Column="0" Padding="0,0,5,0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtAnimationSpeed" Grid.Column="1" Text="10" VerticalAlignment="Center" HorizontalContentAlignment="Center"/>
          </Grid>
        </StackPanel>
      </GroupBox>
      
      <GroupBox Header="Display" Margin="0,0,0,10">
        <StackPanel Margin="5">
          <Grid Margin="0,5">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Content="Line width:" Grid.Column="0" Padding="0,0,5,0" VerticalAlignment="Center"/>
            <Button x:Name="btnDecWidth" Grid.Column="1" Content="  −  " Margin="0,0,5,0" Click="btnDecWidth_Click"/>
            <Button x:Name="btnIncWidth" Grid.Column="2" Content="  +  " Click="btnIncWidth_Click"/>
          </Grid>
        </StackPanel>
      </GroupBox>
      
      <GroupBox Header="Curve info">
        <StackPanel Margin="5">
          <Grid Margin="0,3">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="70"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Num points:" Grid.Column="0" Padding="0"/>
            <TextBox x:Name="txtNumPoints" Grid.Column="1" IsReadOnly="True"/>
          </Grid>
          <Grid Margin="0,3">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="70"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="T min:" Grid.Column="0" Padding="0"/>
            <TextBox x:Name="txtTMin" Grid.Column="1" IsReadOnly="True"/>
          </Grid>
          <Grid Margin="0,3">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="70"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="T max:" Grid.Column="0" Padding="0"/>
            <TextBox x:Name="txtTMax" Grid.Column="1" IsReadOnly="True"/>
          </Grid>
        </StackPanel>
      </GroupBox>
    </StackPanel>
  </Grid>

</Window>
